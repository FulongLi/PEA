"""
Component library for PEA — normalized schema, search, and design links.

Provides a unified interface over the existing SPICE JSON catalog
(``data/spice_models_json/``) and curated reference parts, with a
schema inspired by transistordatabase (upb-lea).
"""
